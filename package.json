{
  "name": "encore-recordings-website",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp -r index.html app.js style.css artists.json images _headers dist/"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
